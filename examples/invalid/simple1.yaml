# yaml-language-server: $schema=../../schema.json
package:
  name: my-package
  version: '0.0.1'
build:
  noarch: python
  script:
    - echo hello world
testaaaa: # this actually fails
  - python:
      imports:
        - polarify
